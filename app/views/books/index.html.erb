<%= render 'users/list', user: @ %> 

    


    <h2>Books</h2>
    <tbody>
    <table class="table">
      <tr>
        <th>Title</th>
        <th>Opinion</th>
      </tr>
      <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.body %></td>
      </tr>
      <% end %>
    </table>
    </tbody>